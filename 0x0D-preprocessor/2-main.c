#include <stdio.h>
#include "main.h"

/**
* main - prints the name of the file it was compiled from, followed by a new line
* @Return: always 0
*/

int main(void)
{
	printf("%s", NAME);
	PRINTF("\n");

	return (0);
}
