#include "main.h"

/**
* mul - calculates the multiple of a and b
* @a: holds value
* @b: holds value
* Return: returns the multiple value
*/

int mul(int a, int b)
{
	int a_mul;

	a_mul = a * b;

	return (a_mul);
}
